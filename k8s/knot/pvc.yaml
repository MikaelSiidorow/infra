apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: knot-data
  namespace: knot
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
