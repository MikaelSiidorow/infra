apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: marginalia-zero-replica-data
  namespace: marginalia
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
